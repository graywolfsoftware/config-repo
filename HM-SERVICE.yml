params:
  datasource:
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
    jdbcUrl: jdbc:sqlserver://SALDEMO06.metratech.com;database=NetMeter;
    username: sa
    password: MetraTech1
    maximumPoolSize: 5
eureka:
  instance:
    hostname: localhost
    nonSecurePort: 8082
